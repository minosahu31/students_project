<%=  simple_form_for @student do |f| %>
<%= f.error_notification %>
	<%= f.input :name %>
	<%= f.input :student_class, label: "class" %>
	<%= f.input :marks  %>
	<%= f.input :description %>
	<%= f.button :submit %>
<% end %>
